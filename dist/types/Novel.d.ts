export default class Novel {
    id: number;
    title: string;
    author: string;
    story: string;
    url: string;
    constructor(id: number, title: string, author: string, story: string, url: string);
}

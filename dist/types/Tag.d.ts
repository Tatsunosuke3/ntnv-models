export default class Tag {
    id: number;
    name: string;
    category: string;
    subcategory: string;
    referenceCount: number;
    constructor(id: number, name: string, category: string, subcategory: string, referenceCount: number);
}
